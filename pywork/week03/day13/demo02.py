'''
写操作
write()--打开一个文件用于写入。若文件存在，打开，并删除原内容，从头进行编辑；若不存在，创建新文件。
writelines()--把参数中的内容全部写入文件中（多行一次性写入）。
'''
# a=open('d:\\a.txt','w')
# a.write('我爱北京天安门')
# a.close()

# ls=['aa','b\n','cc']
# a=open('d:\\a.txt','w')
# a.writelines(ls)
# a.close()